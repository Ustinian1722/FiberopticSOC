name: Electrical OOD optical-gain diagnostic

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "analysis/run_electrical_ood_gain_diagnostic.py"
      - ".github/workflows/electrical-ood-gain-diagnostic.yml"
      - "SiC-18.zip"

permissions:
  contents: read

concurrency:
  group: fiberoptic-electrical-ood-gain
  cancel-in-progress: true

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      OMP_NUM_THREADS: "4"
      MKL_NUM_THREADS: "4"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Stage and verify SiC-18 archive
        shell: bash
        run: |
          test -f SiC-18.zip
          mkdir -p data/raw data/extracted/SiC-18
          cp SiC-18.zip data/raw/SiC-18.zip
          echo "8ebc43eb6d205dfc573dae853d63efcd1a00169be9d2f1e512b9220ea7799bc4  data/raw/SiC-18.zip" | sha256sum -c -
          unzip -q data/raw/SiC-18.zip -d data/extracted/SiC-18

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --disable-pip-version-check numpy==2.3.2 pandas==2.3.2 openpyxl==3.1.5 scikit-learn==1.8.0
          python -m pip install --disable-pip-version-check torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu

      - name: Run causal OOD/gain diagnostic
        run: |
          python analysis/run_electrical_ood_gain_diagnostic.py \
            --data data/extracted/SiC-18 \
            --out-dir results/electrical_ood_gain \
            --window 64 \
            --train-stride 4 \
            --test-stride 1 \
            --epochs 6 \
            --batch-size 256 \
            --envelope-quantile 0.005 \
            --seed 42

      - name: Publish summary
        if: always()
        shell: bash
        run: |
          if [ -f results/electrical_ood_gain/ood_gain_correlations.csv ]; then
            echo "## Electrical OOD vs optical gain" >> "$GITHUB_STEP_SUMMARY"
            echo '```text' >> "$GITHUB_STEP_SUMMARY"
            python -c 'import pandas as pd; print(pd.read_csv("results/electrical_ood_gain/ood_gain_correlations.csv").to_string(index=False)); print(); d=pd.read_csv("results/electrical_ood_gain/ood_gain_summary.csv"); print(d[d.aggregation=="ood_bin"].to_string(index=False))' >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload OOD diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: electrical-ood-optical-gain
          path: results/electrical_ood_gain/**
          if-no-files-found: warn
          retention-days: 30
